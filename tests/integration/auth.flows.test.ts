describe('Auth flows integration', () => {
  it.skip('registers a new account and returns 201', () => {
    expect(true).toBe(true);
  });

  it.skip('rejects duplicate registration attempts', () => {
    expect(true).toBe(true);
  });

  it.skip('logs in with valid credentials and returns JWT payload', () => {
    expect(true).toBe(true);
  });

  it.skip('rejects invalid credentials and enforces progressive lockout', () => {
    expect(true).toBe(true);
  });

  it.skip('denies protected access when bearer token is missing or invalid', () => {
    expect(true).toBe(true);
  });
});
